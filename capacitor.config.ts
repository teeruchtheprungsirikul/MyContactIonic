import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'NewApp.KU.contact',
  appName: 'MyContactAPI',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
